---
title: "T003: Rebuild the compatibility provider payload and usage contract"
type: plan_ticket
task_id: "T003"
parent_plan: "../PLAN.md"
created_at: "2026-04-01T00:13:12Z"
tags: [ticket, plan]
---

# T003: Rebuild the compatibility provider payload and usage contract

**Summary**: Restore the `tinyagent.alchemy_provider` contract exactly, including generic `Model` handling, provider/API/base-URL resolution, tool serialization, and required `usage` plus `usage.cost` validation on final assistant messages.

**Owner**: runtime

**Estimate**: 8h

**Dependencies**: T001,T002

**Target milestone**: M2

**Acceptance test**: `uv run pytest tests/test_alchemy_provider.py tests/test_usage_contracts.py -q`

**Files/modules touched**:
- tinyagent/alchemy_provider.py
- rust/src/lib.rs
- rust/src/types.rs
- tests/test_alchemy_provider.py
- tests/test_usage_contracts.py
- docs/api/providers.md
- docs/api/usage-semantics.md

**Steps**:
1. Preserve `OpenAICompatModel` and the compatibility adapter's normalization rules for `provider`, `api`, `base_url`, headers, reasoning, and env-var API-key lookup.
2. Ensure serialized `model`, `context`, and `options` payloads match the dict shapes currently passed into the native binding.
3. Keep assistant-message validation strict enough to reject missing `usage`, malformed `usage.cost`, and non-dict payloads at the compatibility boundary.
